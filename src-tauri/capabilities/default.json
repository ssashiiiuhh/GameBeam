{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions for main and overlay windows",
  "windows": [
    "main",
    "overlay"
  ],
  "permissions": [
    "core:default",
    "store:default",
    "global-shortcut:default"
  ]
}
